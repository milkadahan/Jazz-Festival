# הצעת מיתוג לאתר — פסטיבל הג'אז של ים סוף, מהדורה 40 (2026)

**מקור:** `Graphig assets/poster-2026.jpg` (900×1600, הפוסטר הרשמי המאושר)
**תאריך:** 2026-08-02 · **מחבר:** דיזי המעצב
**סטטוס:** הצעה לעיון בלבד — לא בוצע שום שינוי בקוד. אף `.html` / `.css` לא נגע.

---

## 0. תקציר מנהלים

הפוסטר הוא פוסטר-נסיעות רטרו **קר-דומיננטי**: 40% משטח הקנבס הוא תכלת-טורקיז בהיר, ועוד 14% כחול-נייבי עמוק בפוטר. החום (קורל, זהב, שקיעה) הוא **אקסנט**, לא רקע.

האתר היום הוא בדיוק ההפך — **חם-דומיננטי**: רקע שמנת `#FAF4E9` + כתום `#E0560B` שמופיע 251 פעם בקוד.

זו נקודת המתח המרכזית בכל ההצעה. **המסקנה: לא להפוך את האתר לטורקיז.** משטח הקריאה נשאר שמנת חמה (קריאוּת + 251 מופעים של הכתום שאין סיבה לשבור). מה שכן משתנה: שכבת ה-**עוגנים והאקסנטים** — הכהה הנוכחי (`#1a1410` חום-שחור) מוחלף בנייבי של הפוסטר, נוסף קורל כצבע זהות למהדורת ה-40, ושלושה מוטיבים גרפיים מהפוסטר (קצה נייר קרוע, טבעות שמש, קו רכס) הופכים לרכיבי רקע חוזרים.

---

## 1. פלטת הצבעים — דגימה בפועל מקובץ התמונה

הדגימה בוצעה ב-Python/Pillow: median של פאץ' לכל אזור + מסכות לפי גוון (cream / coral / yellow) + סריקות קו אופקיות ואנכיות דרך קשתות השמש + כימות MEDIANCUT לפלטה הדומיננטית.

### 1.1 הערה קריטית — טקסטורת הנייר

לפוסטר יש טקסטורת נייר/גריין שמורידה ~8–10% בהירות ו-~10–15% רוויה מכל צבע. חשוב במיוחד: **ה"לבן" של הפוסטר הוא ניטרלי-קר** — `#DAD9DA` (R218 G217 B218, נטול חום לחלוטין), ולא שמנת. כל החום בפוסטר מגיע מפס הזהב העליון ומהשקיעה, לא מהנייר.

זה אומר שהשמנת החמה של האתר (`--bg #FAF4E9`, `--on-accent #FFFDF8`) **אינה** מה שרואים בפוסטר. ההמלצה בהמשך: להשאיר את השמנת החמה כמשטח האתר, ולהוסיף טוקן `--paper` ניטרלי לשימוש ממוקד מעל משטחים צבעוניים/כהים, שם ה"דיו הלבן" של הפוסטר הוא הנכון.

### 1.2 טבלת הצבעים המלאה

| # | תפקיד בפוסטר | דגימה גולמית | ערך מומלץ לווב | הערה |
|---|---|---|---|---|
| 1 | **שמי טורקיז** (הרקע הראשי) | `#32B2D9` (בהיר ביותר `#35B8DE`) | **`#35B8DE`** | 40% משטח הפוסטר. משטח בלבד — נכשל כטקסט על שמנת (2.12:1) |
| 2 | **פס זהב/חרדל עליון** | `#D8AF54` (רווי `#DBB354`) | **`#DCAE55`** | אוכרה מעומעמת, לא צהוב זרחני |
| 3 | **"40" — קרם/לבן** | `#DAD9DA` (p90 `#E1DFE0`) | **`#E3E1E2`** | ניטרלי-קר. לא שמנת! |
| 4 | **קו מתאר קורל של ה-"40"** | `#CA6663` | **`#D5645F`** | היסט צל תלת-ממדי, לא stroke |
| 5 | **קורל של הבאדג'ים** | `#CF6460` (רווי `#D5645F`) | **`#D5645F`** | הצבע המזוהה ביותר של הפוסטר |
| 6 | **טקסט קרם על באדג'** | `#DEDAD9` | `#E3E1E2` | יחס 3.55:1 — טקסט גדול בלבד |
| 7 | **קשת שמש — זהב חיצוני** | `#E9BD58` | **`#E9BD58`** | הטבעת הרחבה ביותר |
| 8 | **קשת שמש — כתום אמצע** | `#DE8047` | **`#DE8047`** | |
| 9 | **קשת שמש — ליבת קורל** | `#CE4743` | **`#CE4743`** | הדיסק הפנימי |
| 10 | **הר שמאלי — כתום שרוף** | `#BA4B25` | `#BA4B25` | בן-דוד עמוק של `--orange` הקיים |
| 11 | **הר ימני — זהב-כתום** | `#D37D1A` | `#D37D1A` | |
| 12 | **רכסים ארגמן** | `#A12539` / `#9D2136` | **`#A12539`** | 6.76:1 על שמנת — עובד כטקסט! |
| 13 | **מסת הר כהה** | `#7E2136` | `#7E2136` | 6.27% מהקנבס |
| 14 | **קצה הר כהה ביותר** | `#630C21` | `#630C21` | |
| 15 | **צללית מנוף הנמל** | `#5A173A` / `#4F0D31` | **`#4F1233`** | שזיף כהה, לא שחור |
| 16 | **צלליות נמל/מגדלור/דקלים** | `#402043` / `#992238` | `#402043` | |
| 17 | **רצועת מים** | `#1C7294` | **`#1C7294`** | 4.94:1 על שמנת — AA לטקסט |
| 18 | **פוטר נייבי עמוק** | `#0D386C` | **`#0D386C`** | 10.66:1 על שמנת — עוגן מצוין |
| 19 | **טקסט תאריך צהוב** | `#DCD25E` | **`#DFD46A`** | 7.65:1 על נייבי — AAA |
| 20 | **טקסט קרדיט על הזהב** | `#5F2134` | `#5F2134` | ברדוקס למשפחת ה-maroon |

### 1.3 הפלטה הדומיננטית (כימות ל-20, כל הקנבס)

```
#CF5B55  9.16%   קורל          #DCB76C  7.31%   זהב
#31B2D9  8.35%   טורקיז        #7E2136  6.27%   ארגמן כהה
#0D366A  8.29%   נייבי         #DEDDDC  6.04%   קרם ניטרלי
#35B8DE  7.55%   טורקיז בהיר   #D4873D  4.47%   כתום שקיעה
```

**המסקנה המספרית:** טורקיז (~34% מצטבר) > קורל (~9%) ≈ נייבי (~8%) ≈ זהב (~7%). זה **לא** פוסטר כתום.

---

## 2. השפה הוויזואלית והאווירה

### 2.1 טיפוגרפיה

**"40" (הכותרת הראשית).** ספרות דיספליי גיאומטריות כבדות במשקל אחיד, בנויות מקווי קרם עבים שבתוכם רץ **קו-פנים דק בקורל** (inline-stroke — טכניקת שילוט רטרו של שנות ה-50/60), ובנוסף **היסט צל בקורל** לכיוון ימין-למטה. ה-"0" הוא עיגול כמעט מושלם עם פתח בצד. המילה "מהדורה" בעברית משובצת בתוך הקערה של ה-0 — פתרון קומפוזיציוני חכם.

**"RED SEA JAZZ FESTIVAL".** סאנס דיספליי כבד, מעט מעובה/מעוגל-קונדנסד, עם **טרמינלים שטוחים וקאונטרים מרובעים** — אופי סטנסיל/שילוט תעשייתי. שתי תובנות שקל לפספס:
- **היררכיה בתוך שורה אחת**: "JAZZ" בערך פי 2 בגובה מ-"RED SEA" ו-"FESTIVAL", שיושבות על אותו baseline. זה לא tracking — זו קפיצת גודל.
- **קווים תחתיים דקורטיביים** מתחת לאותיות בודדות (E, A, S, T, L) — רמז מכונת-כתיבה/חותמת. פרט קטן שהוא חלק מזהות הכותרת.
- אותו היסט צל קורל כמו ב-"40".

**עברית.** גרוטסק עברי כבד, גיאומטרי, טרמינלים שטוחים, ללא קונטרסט משיכה. בבאדג'ים ובשורת התאריך — משקל שחור/כבד. בקרדיט העליון — משקל רגיל.

**מספרים ותאריכים.** `11-14/11/2026` בסאנס גיאומטרי קונדנסד בצהוב-לימוני רך על נייבי. אותו סאנס גם ב-URL בקרם.

### 2.2 סגנון האיור

פוסטר-נסיעות שטוח (flat vector), **ללא גרדיאנטים בכלל** — כל המעברים הם **פסים אטומים בצבע מלא**. שלושה מנגנונים:
1. **קשתות קונצנטריות** — חצאי-עיגולים מקוננים (זהב → זהב-חיוור → כתום → ליבת קורל) שיוצרים "שמש שוקעת" מוגדלת, מלאכותית ומאוד גרפית.
2. **צלליות בשכבות** — הרכסים, המנוף, המגדלור, הדקלים והנמל בנויים כשכבות אטומות בעומקי אדום-שזיף הולכים ומתכהים. אין קווי מתאר, אין טקסטורה בתוך הצלליות.
3. **פסי אופק אופקיים** — הפרדות בין שמים / הרים / מים / פוטר הן קווים אופקיים ישרים ונקיים.

### 2.3 טקסטורה

שתי שכבות נפרדות שקל לבלבל ביניהן:
- **גריין נייר גלובלי** — סיבי נייר עדינים על כל השטח, מוריד רוויה, נותן תחושת הדפסה ישנה.
- **קצה נייר קרוע** — לא אפקט; זו **צורה גרפית ממש**: מתחת לפס הזהב העליון (ומתחת לשורת הבאדג'ים) יש קו קרע לא-סדיר עם רצועת "סיבים" בקרם ניטרלי (`#D8D7D8`) שחושפת את השכבה שמתחת. זה המוטיב החוזר הכי מובהק בפוסטר.

### 2.4 האווירה — במילים מדויקות

**שטוף-שמש · נוסטלגי-הדפסי · תיירותי-חופי · חגיגי-אך-מאופק · תעשייתי-נמלי · ידני**

הבהרה על שני קצוות: זה **לא** "אלגנטי/פרימיום" (הצבעים רוויים והצורות בוטות) וזה גם **לא** "צעיר/ניאון" (הכל מעומעם בטקסטורה, הזהב אוכרה ולא צהוב). המקום המדויק: **תיירות ים-תיכונית של שנות ה-60, מודפסת בסילקסקרין**. הנוכחות של המנוף התעשייתי לצד הדקלים היא מה שמונע מזה להיות פוסטר חופשה גנרי — זה נמל אילת ספציפית.

---

## 3. מפת יישום למערכת ה-CSS הקיימת

### 3.1 מה מצאתי בקוד (מצב קיים)

| ממצא | פירוט |
|---|---|
| `:root` משוכפל | 12 קבצי HTML, כל אחד עם `:root` משלו (2–4 בלוקים לקובץ). **אין קובץ CSS משותף** — כל שינוי טוקן = 12 עריכות |
| `--orange #E0560B` | **251 מופעים** — הטוקן הכי מושרש באתר |
| `--red #D62246` | 46 מופעים · `--border-strong` 49 · `--teal #0A8F8E` 28 · `--btn-gradient` 22 |
| `--warm-glow #FF8C42` | **0 מופעים** — טוקן מת, זמין למחזור |
| רקעי Hero **לא עקביים** | `about/contact/past-artists` = `#1a1410` כהה · `faq/merch/plaza` = `var(--bg)` שמנת · `lineup` = `#f5efe6` + תמונה · `index #hero` = `#FAF4E9` |
| עוגן כהה נוסף | `#2d2726` (טיקר עליון + פוטר) ב-13 קבצים |
| `.divider` | `1px` gradient שקוף→`--border`→שקוף — מפריד ניטרלי לגמרי |
| `border-radius` | `50px` (פילים) — 72 מופעים, הצורה השלטת באתר |
| **תקדים קיים לקצה קרוע** | `.featuring-pill-deco--left/right` ב-`index.html` + `past-artists.html`: `mask-image` עם SVG path קרוע כ-data-URI. **התשתית למוטיב הנייר הקרוע כבר קיימת ועובדת** |
| פונטים | `--font: 'Google Sans', 'Hepta Slab', 'Noto Sans Hebrew', serif`. Google Sans Variable מתארח מקומית ב-`Graphig assets/fonts/`, Hepta Slab + Noto Sans Hebrew מ-Google Fonts |

### 3.2 טוקנים קיימים — לשנות ערך

| טוקן | ערך נוכחי | ערך מוצע | נימוק | סיכון |
|---|---|---|---|---|
| `--yellow` | `#E0A800` | **`#DCAE55`** | הזהב של הפוסטר הוא אוכרה מעומעמת, לא ענבר רווי. 15 מופעים בלבד | נמוך |
| `--red` | `#D62246` | **`#C02A41`** | ה-`#D62246` הוא ארגמן-מגנטה; בפוסטר אין מגנטה בכלל. `#C02A41` נע לעבר `#A12539` של הרכסים ושומר בהירות לגרדיאנט הכפתורים. 5.26:1 על שמנת (השתפר מ-4.4) | בינוני — 46 מופעים |
| `--teal` | `#0A8F8E` | **`#0E7B9C`** | הטורקיז הנוכחי ירקרק; בפוסטר הטורקיז כחלחל (`#1C7294` מים / `#35B8DE` שמים). 5.0:1 על שמנת | בינוני — 28 מופעים |
| `--teal-text` | `#086665` | **`#0A5C77`** | יישור לגוון החדש | נמוך |
| `--warm-glow` | `#FF8C42` | **`#DE8047`** | טוקן מת (0 מופעים) — למחזר ככתום השקיעה של הפוסטר | אפס |
| `--shadow` / `--shadow-strong` | `rgba(58,42,30,…)` | `rgba(13,56,108,0.10)` / `…0.16` | להעביר את גוון הצל מחום לנייבי — יוצר קוהרנטיות עם העוגן החדש בלי שינוי ויזואלי דרמטי | נמוך |

### 3.3 טוקנים קיימים — **לא** לשנות

| טוקן | ערך | למה משאירים |
|---|---|---|
| `--orange` | `#E0560B` | 251 מופעים. יושב בנוחות בתוך קשת השקיעה (בין `#DE8047` ל-`#BA4B25`). לשנות אותו = לשבור את כל שכבת האינטראקציה תמורת רווח מיתוגי אפסי |
| `--bg` / `--bg2` / `--card-bg` | `#FAF4E9` / `#F3E9D6` / `#FFFDF8` | משטח הקריאה של האתר. הנייר של הפוסטר ניטרלי-קר ולא מתאים לקריאת גוף טקסט ארוכה |
| `--text` / `--muted` | `#3A2A1E` / `#6A5540` | קונטרסט תקין, ואין בפוסטר צבע טקסט שמתחרה |
| `--pink` | `#E0568F` | 20 מופעים; חורג מהפוסטר אבל לא מזיק. להוציא מהדרגה בהדרגה, לא למחוק עכשיו |

### 3.4 טוקנים חדשים להוספה

```css
/* ── שכבת מהדורת ה-40 (מהפוסטר הרשמי) ──────────────── */
--coral:        #D5645F;  /* צבע הזהות של המהדורה — באדג'ים, אייברואו, מוטיבים */
--coral-deep:   #C4514C;  /* אותו קורל, מוכהה — לטקסט קטן ולמצבי hover */
--sky:          #35B8DE;  /* שמי הפוסטר — משטחים בלבד, אף פעם לא טקסט */
--sea:          #1C7294;  /* רצועת המים — עובר AA כטקסט על שמנת */
--navy:         #0D386C;  /* פוטר הפוסטר — העוגן הכהה החדש של האתר */
--gold:         #DCAE55;  /* פס הזהב העליון (זהה ל---yellow המוצע) */
--gold-bright:  #E9BD58;  /* טבעת השמש החיצונית */
--sunset:       #DE8047;  /* טבעת השמש האמצעית */
--sun-core:     #CE4743;  /* ליבת השמש */
--crimson:      #A12539;  /* רכסי ההרים — 6.76:1 על שמנת, כשר לטקסט */
--maroon:       #7E2136;  /* מסת ההר הכהה */
--plum:         #4F1233;  /* צלליות מנוף/מגדלור/דקלים */
--paper:        #E3E1E2;  /* הדיו-הלבן הניטרלי של הפוסטר (≠ --on-accent החמה) */
--sun-yellow:   #DFD46A;  /* צהוב התאריך — 7.65:1 על --navy (AAA) */

/* ── גרדיאנט השקיעה — המוטיב המרכזי ──────────────── */
--sunset-arc: radial-gradient(circle at 50% 100%,
                var(--sun-core)   0%,  var(--sun-core)  22%,
                var(--sunset)    22%,  var(--sunset)    38%,
                var(--gold-bright) 38%, var(--gold-bright) 52%,
                transparent      52%);
```

**שים לב:** ה-`--sunset-arc` בנוי בכוונה עם **עצירות קשות** (אותו אחוז פעמיים) כדי לשחזר את הפסים האטומים של הפוסטר. גרדיאנט רך היה מחמיץ את כל האופי.

### 3.5 טבלת ניגודיות (WCAG 2.1) — כל הצירופים שנבדקו

| קדמה | רקע | יחס | תקן | פסיקה |
|---|---|---|---|---|
| `--navy #0D386C` | `--bg #FAF4E9` | **10.66** | AAA | עוגן מצוין. מחליף את `#1a1410` |
| `--paper #E3E1E2` | `--navy #0D386C` | **8.97** | AAA | טקסט על ה-hero הכהה |
| `--sun-yellow #DFD46A` | `--navy #0D386C` | **7.65** | AAA | בדיוק כמו שורת התאריך בפוסטר |
| `--crimson #A12539` | `--bg` | **6.76** | AA | כשר לטקסט קטן |
| `--text #3A2A1E` | `--gold #DCAE55` | **6.69** | AA | ככה לכתוב על משטח זהב |
| `--gold #DCAE55` | `--navy` | **5.68** | AA | |
| `--text` | `--sky #35B8DE` | **5.92** | AA | טקסט על משטח שמים ✔ |
| `--red #C02A41` (מוצע) | `--bg` | **5.26** | AA | שיפור מ-`#D62246` |
| `--sky #35B8DE` | `--navy` | **5.04** | AA | |
| `--sea #1C7294` | `--bg` | **4.94** | AA | |
| `--on-accent` | `--coral-deep #C4514C` | 4.46 | AA-large | **לטקסט קטן על קורל — חובה `--coral-deep`** |
| `--sun-yellow` | `--sea` | 3.54 | AA-large | |
| `--on-accent` | `--coral #D5645F` | **3.55** | AA-large | כמו הבאדג'ים בפוסטר — טקסט גדול+כבד בלבד |
| `--orange #E0560B` | `--bg` | 3.48 | AA-large | ללא שינוי מהמצב הקיים |
| `--coral` | `--bg` | 3.30 | AA-large | |
| `--sky` | `--bg` | **2.12** | **נכשל** | **משטח בלבד — לעולם לא טקסט** |
| `--gold` | `--bg` | **1.88** | **נכשל** | **משטח בלבד** |
| `--paper #E3E1E2` | `--coral` | **2.67** | **נכשל** | הבאדג' של הפוסטר עצמו לא היה עובר בטקסט קטן |

---

## 4. רקעים ו-Hero

### 4.1 העיקרון: שלוש רמות רקע, לא אחת

היום ה-hero מפוצל בין שלושה טיפולים שונים ללא היגיון. הפוסטר נותן היררכיה מוכנה — **שמים למעלה, שקיעה באמצע, מים למטה** — שאפשר למפות ישירות על מבנה הדף:

| רמה | טיפול | איפה |
|---|---|---|
| **A — עוגן כהה** | `--navy #0D386C` + זוהר עדין | Hero של דפים פנימיים (`about`, `contact`, `past-artists`) — מחליף `#1a1410`. גם הטיקר והפוטר (`#2d2726` → `--navy`) |
| **B — שמים** | `--sky #35B8DE` + קצה נייר קרוע בתחתית | סקשן אחד ויחיד בדף הבית: ה-hero הראשי / רצועת התאריכים. **לא יותר מזה** |
| **C — שמנת** | `--bg` ללא שינוי | כל שאר הדף: תוכן, כרטיסים, טבלאות, כתבות |

**נגד ההצפה:** אין לפזר טורקיז על סקשנים רבים. הטורקיז בפוסטר עובד כי הוא **רקע רציף אחד** שהתוכן צף מעליו. שלושה סקשנים טורקיז מפוזרים ייראו כמו באג, לא כמו מיתוג.

### 4.2 החלפת הזוהר הרדיאלי הקיים

הרקע הנוכחי של ה-hero הכהה:

```css
/* קיים — about.html:364, contact.html:365, past-artists.html:385 */
background: radial-gradient(ellipse at 30% 60%, rgba(224,86,11,0.25) 0%, transparent 60%),
            radial-gradient(ellipse at 75% 40%, rgba(10,143,142,0.18) 0%, transparent 55%),
            radial-gradient(ellipse at 55% 80%, rgba(214,34,70,0.15) 0%, transparent 50%);
```

מוצע — אותו מבנה בדיוק (ולכן שינוי בטוח וזול), בצבעי הפוסטר, על `--navy`:

```css
background: radial-gradient(ellipse at 50% 115%, rgba(233,189,88,0.32) 0%, transparent 55%),  /* זוהר שמש מלמטה */
            radial-gradient(ellipse at 25% 30%,  rgba(53,184,222,0.20) 0%, transparent 60%),  /* שמים */
            radial-gradient(ellipse at 78% 70%,  rgba(213,100,95,0.22) 0%, transparent 55%);  /* קורל */
```

הזוהר **מלמטה-מרכז** (`50% 115%`) הוא הפרט שעושה את ההבדל — הוא מדמה את השמש השוקעת מאחורי האופק במקום זוהר צף אקראי.

### 4.3 קשת השמש כאלמנט רקע חוזר

זהו האלמנט הגרפי היחיד בפוסטר שאפשר לשכפל **ב-CSS טהור בלי שום נכס** (ראה `--sunset-arc` בסעיף 3.4). שימוש: `div` דקורטיבי `position:absolute; bottom:0; left:50%; transform:translateX(-50%); width:min(720px,120%); aspect-ratio:2/1; opacity:.85; pointer-events:none;` בתוך hero/CTA עם `overflow:hidden`.

זול, ניתן לתמה, ומכיל את מלוא ה-DNA של הפוסטר.

---

## 5. שימוש בצבע אקסנט — קורל מול כתום

**התשובה: משלים, לא מחליף.** עם חלוקת תפקידים מוגדרת:

| שכבה | צבע | תפקיד | דוגמאות |
|---|---|---|---|
| **שכבת זהות** (חדשה) | `--coral #D5645F` | "זו מהדורת ה-40". סטטי, לא-אינטראקטיבי | באדג'ים, אייברואו של סקשנים, צל-היסט של כותרות, מפרידי נייר קרוע, תגיות במות/ז'אנר |
| **שכבת אינטראקציה** (קיימת) | `--orange #E0560B` | "אפשר ללחוץ". דינמי | כפתורים, קישורים, hover, focus, active, `--btn-gradient` |
| **שכבת עומק** | `--crimson` / `--maroon` / `--plum` | טקסט על משטחים חמים, צלליות, מצבי hover כהים | |

**הנימוק:** לכתום יש קונטרסט טוב יותר (3.48 מול 3.30), הוא כבר מקודד ב-`--btn-gradient` ובכל מצבי ה-focus, והוא **קיים בפוסטר** בתור טבעת השקיעה `#DE8047` והרכס `#BA4B25` — הוא לא זר. להעביר 251 מופעים לקורל זה עבודה גדולה, סיכון רגרסיה, ורווח מיתוגי שלילי (הקורל יאבד את הייחוד שלו אם הוא יהפוך לצבע של כל כפתור).

### 5.1 באדג'ים — הפרט הכי חשוב ליישום

הבאדג'ים של הפוסטר ("4 ימים" / "40 מופעים" / "4 במות") הם **מלבנים כמעט חדים** — רדיוס ~2–4px. האתר משתמש ב-`border-radius: 50px` ב-72 מקומות.

זה לא ניואנס — זה ההבדל בין "פוסטר הדפס" ל"אפליקציה". **המלצה:** להשאיר את הפילים העגולים לשכבת האינטראקציה (כפתורים, טאבים, פילטרים), ולהגדיר וריאנט חדש `.badge--poster` עם `border-radius: 3px` + `background: var(--coral)` + `color: var(--on-accent)` + `font-weight: 800` לשכבת הזהות בלבד: תגיות סטטיסטיקה, ספירת ימים/מופעים/במות, תגי "Headliner".

⚠️ קונטרסט קרם-על-קורל הוא 3.55:1 → **טקסט גדול וכבד בלבד** (≥18.66px bold). לטקסט קטן על קורל — `--coral-deep #C4514C` (4.46:1).

### 5.2 קישורים

הקישורים היום יורשים `--orange`. אין סיבה לשנות. אם רוצים חתימה של הפוסטר: `text-decoration-color: var(--coral)` עם `text-underline-offset: 3px` — מהדהד את הקווים התחתונים הדקורטיביים מתחת לאותיות ב-"FESTIVAL".

---

## 6. מוטיבים חוזרים

מדורג לפי יחס תמורה/מאמץ:

### 6.1 ⭐ קצה נייר קרוע — **עדיפות עליונה**

זה המוטיב המזוהה ביותר של הפוסטר, **והתשתית לו כבר קיימת בקוד**: `.featuring-pill-deco--left/right` משתמשות ב-`mask-image` עם SVG path קרוע כ-data-URI ו-`background: var(--pill-bg)` — כך שצבע אחד שולט גם בגוף וגם בקצה. הדפוס הזה מתועד ועובד.

**היישום:** אלמנט `.torn-edge` ברוחב מלא, גובה 12–18px, `mask-image` עם path קרוע אופקי `preserveAspectRatio="none"`, `background: var(--bg)` — ממוקם ב-`bottom:-1px` של כל סקשן צבעוני. הסקשן שמעל נראה כאילו נקרע מהנייר של הסקשן שמתחת.

**שדרוג מיידי:** להחליף את `.divider` הקיים (`1px` gradient ניטרלי, מפריד חסר-אופי) בגרסת נייר קרוע. שינוי במקום אחד → משפיע על כל האתר.

### 6.2 ⭐ קשת השמש הקונצנטרית

CSS טהור, אפס נכסים (סעיף 4.3). לשימוש ברקע של: ה-hero הראשי, ה-CTA התחתון של `lineup.html`, וכרקע-רפאים ב-`opacity:.12` מאחורי מספרי סטטיסטיקה גדולים (`#about-strip`).

### 6.3 קו רכס הרים

SVG יחיד ברוחב מלא, 3 שכבות path (`--crimson` / `--maroon` / `--plum`), גובה 60–90px. מיקום: מעל הפוטר, כמעבר מתוכן לעוגן הנייבי. זה בדיוק התפקיד שהוא ממלא בפוסטר.

### 6.4 צללית המנוף

**להשתמש בזהירות ובנקודה אחת בלבד.** המנוף הוא הפרט שממקם את הפסטיבל בנמל אילת ולא בחוף גנרי — אבל הוא גם הפרט הקריא ביותר, ולכן חוזר עליו הופך אותו לקישוט. הצעה: `plaza.html` (עמוד הרחבה המרכזית — הקשר הנמל ישיר) או ה-hero של `about.html`.

### 6.5 גריין הנייר

`background-image` של רעש דק ב-`opacity: .03–.05` על `body`. **המלצה: לדחות.** התמורה הוויזואלית נמוכה, והעלות בביצועים ובחדות טקסט (במיוחד בעברית על מסכי retina) לא מוצדקת. אם בכל זאת — רק על סקשני ה-hero, לא על `body`.

---

## 7. טיפוגרפיה

### 7.1 מה טעון היום

```
--font: 'Google Sans', 'Hepta Slab', 'Noto Sans Hebrew', serif
```
- **Google Sans Variable** — מקומי, `Graphig assets/fonts/` (לטיני + עברי, Regular + Italic)
- **Hepta Slab** (100–900) + **Noto Sans Hebrew** (300–900) — Google Fonts

### 7.2 האבחנה

**Hepta Slab הוא slab-serif. הכותרת בפוסטר היא סאנס גיאומטרי-סטנסילי כבד.** אלה שתי משפחות שונות. Hepta Slab רטרו מספיק כדי "לא לצרום", אבל הוא לא מדבר את השפה של הפוסטר — הסריפים שלו מוסיפים עידון עיתונאי, בעוד הפוסטר בוטה ושילוטי.

**אזהרה מתועדת:** Hepta Slab **חסר גליפים עבריים**. כל override של `font-family: 'Hepta Slab', serif` על אלמנט שעלול להכיל עברית = טקסט עברי שנופל ל-Times. זו תקלה שכבר קרתה ב-`lineup.html` (`.view-toggle-btn.active`) ותוקנה. כל פונט דיספליי חדש חייב stack דו-לשוני מפורש.

### 7.3 ההמלצה — פונט דיספליי שלישי, ממוקד

**לא להחליף** את `--font` (טקסט גוף — Google Sans מצוין וגם מקומי, כלומר מהיר). **להוסיף** טוקן שלישי לכותרות בלבד:

```css
--font-display: 'Anton', 'Alfa Slab One', 'Hepta Slab', 'Noto Sans Hebrew', sans-serif;
```

מועמדים (Google Fonts, חינמיים, כולם דורשים בדיקת גליפים עבריים):

| מועמד | התאמה לפוסטר | הערה |
|---|---|---|
| **Anton** | ⭐⭐⭐ קונדנסד כבד, טרמינלים שטוחים — הכי קרוב ל-"RED SEA JAZZ" | לטיני בלבד. **חובה fallback עברי** |
| **Archivo Black** | ⭐⭐ גרוטסק כבד רחב | לטיני בלבד |
| **Alfa Slab One** | ⭐⭐ סלאב רטרו כבד, אווירת פוסטר | לטיני בלבד |
| **Rubik One / Rubik Black** | ⭐⭐⭐ **עברית + לטינית באותה משפחה**, גיאומטרי, טרמינלים שטוחים | הבחירה הבטוחה ביותר לאתר דו-לשוני |

**המלצתי:** `Anton` ללטינית + `Rubik` (משקל 800/900) לעברית, כ-stack אחד. זה משחזר את המצב האמיתי של הפוסטר — שני פונטים שונים ללטינית ולעברית, ששניהם כבדים וגיאומטריים.

### 7.4 אפקטים טיפוגרפיים לשחזור

שני האפקטים של הפוסטר, שניהם CSS טהור, שניהם זולים:

```css
/* היסט צל קורל — החתימה של הכותרת בפוסטר */
.title--poster { text-shadow: 3px 3px 0 var(--coral); }

/* קו-פנים (inline stroke) — לכותרות ענק בלבד */
.title--inline {
  color: var(--paper);
  -webkit-text-stroke: 1px var(--coral);
  paint-order: stroke fill;
}
```

⚠️ `-webkit-text-stroke` על טקסט עברי בגדלים קטנים סותם קאונטרים. **מעל 64px בלבד.**

---

## 8. איפה רואים את זה קודם — 3 דוגמאות קונקרטיות

### דוגמה 1 — Hero של `past-artists.html` / `about.html` / `contact.html`

הכי גבוה בתמורה, הכי נמוך בסיכון: שלושת הדפים חולקים בלוק CSS כמעט זהה, כך שזו **עריכה אחת משוכפלת שלוש פעמים**.

| | היום | מוצע |
|---|---|---|
| רקע | `#1a1410` חום-שחור | `--navy #0D386C` |
| זוהר | 3 רדיאלים כתום/טיל/אדום | 3 רדיאלים זהב-מלמטה / שמים / קורל (סעיף 4.2) |
| `.page-hero-tag` | פיל `50px`, `rgba(250,244,233,0.08)` | מלבן `3px` `--coral` + `--on-accent` |
| כותרת | gradient כתום→אדום על החלק המודגש | `--paper` + `text-shadow: 3px 3px 0 var(--coral)` |
| תחתית הסקשן | חתך ישר | `.torn-edge` — נקרע אל תוך השמנת |

**התוצאה:** מהחום-שחור הכללי הנוכחי לרצועה כחולה-עמוקה שמזוהה מיידית עם הפוסטר, בלי לגעת בשום דבר מתחת ל-hero.

### דוגמה 2 — רצועת הסטטיסטיקות בדף הבית (`#about-strip` / `.about-stats`)

"40 שנות פסטיבל · 4 ימים · 4 במות · 40+ הופעות · +20k מבקרים" — **זה בדיוק אותו תוכן של שורת הבאדג'ים בפוסטר.** ההזדמנות המובהקת ביותר באתר.

| | היום | מוצע |
|---|---|---|
| `.stat-num` | gradient `--orange`→`--yellow` | `--coral` מלא, `font-weight: 900`, `--font-display` |
| מפרידים | `1px` `--border` | להחליף ב-3 באדג'ים מלבניים (`radius: 3px`, רקע `--coral`, טקסט `--on-accent`) בדיוק כמו הפוסטר |
| רקע הרצועה | שמנת | קשת שמש (`--sunset-arc`) ב-`opacity:.14`, ממורכזת מלמטה |

### דוגמה 3 — הפוטר + הטיקר (`footer.js`, נקודת עריכה **אחת** → כל 12 הדפים)

`footer.js` מזריק את הפוטר לכל הדפים דרך IIFE יחיד. שינוי כאן מתפשט לאתר כולו בעריכה בודדת.

| | היום | מוצע |
|---|---|---|
| רקע פוטר | `#2d2726` חום-שחור | `--navy #0D386C` — זה **בדיוק** מה שהפוסטר עושה בתחתית |
| מעל הפוטר | כלום | SVG קו רכס 3 שכבות (`--crimson`/`--maroon`/`--plum`), גובה ~72px |
| שורת התאריכים | טקסט כללי | `--sun-yellow #DFD46A` על `--navy` — **7.65:1, AAA**. שחזור מדויק של "11-14/11/2026 בנמל אילת" |
| טיקר עליון | `#2d2726` | `--navy` — מסגר את הדף בין שני פסי נייבי, בדיוק כמו הפוסטר |
| גרדיאנט כפתור ניוזלטר | `#E0560B → #D62246` | `#E0560B → #C02A41` |

---

## 9. סיכונים ומה לא לעשות

| ⚠️ | פירוט |
|---|---|
| **אין קובץ CSS משותף** | כל טוקן מוגדר ב-12 קבצי HTML נפרדים. כל שינוי בסעיף 3.2 = 12 עריכות מסונכרנות. **המלצה חזקה: לחלץ `:root` ל-`tokens.css` יחיד לפני שמתחילים** — אחרת האתר ייצא ממצב מסונכרן תוך שבוע |
| **לא להטביע את האתר בטורקיז** | סקשן שמים אחד, לא חמישה. הטורקיז בפוסטר עובד כרקע רציף יחיד |
| **`--sky` ו-`--gold` נכשלים בקונטרסט** | 2.12:1 ו-1.88:1 על שמנת. **משטחים בלבד. לעולם לא טקסט, לא אייקונים, לא גבולות פעילים** |
| **קרם על קורל = 3.55:1** | הבאדג'ים של הפוסטר עצמם לא היו עוברים בטקסט קטן. טקסט גדול+כבד בלבד, אחרת `--coral-deep` |
| **Hepta Slab ללא גליפים עבריים** | כל `font-family` שמסתיים ב-`serif` בלי fallback עברי = באג ודאי בעברית. תקלה מתועדת שכבר קרתה |
| **לא לגעת ב-`--orange`** | 251 מופעים. הכתום נשאר צבע האינטראקציה |
| **לא לפזר את המנוף** | מוטיב חד-פעמי. חזרה עליו הופכת אותו מסימן-היכר לקישוט |
| **גריין נייר על `body`** | לדחות. פוגע בחדות טקסט עברי ובביצועים, תמורה נמוכה |

---

## 10. סדר ביצוע מוצע

| שלב | פעולה | היקף | תמורה |
|---|---|---|---|
| **0** | חילוץ `:root` ל-`tokens.css` משותף | 12 קבצים | תשתית — חובה לפני הכל |
| **1** | הוספת 14 הטוקנים החדשים (סעיף 3.4) | תוספת בלבד, לא שוברת | אפס סיכון |
| **2** | פוטר + טיקר → `--navy` (`footer.js`) | קובץ אחד | **גבוהה מאוד** — כל האתר |
| **3** | 3 ה-hero הכהים → `--navy` + זוהר שקיעה | 3 קבצים | גבוהה |
| **4** | `.divider` → קצה נייר קרוע | כל האתר | גבוהה |
| **5** | רצועת הסטטיסטיקות → באדג'י פוסטר | `index.html` | גבוהה |
| **6** | רי-טיון `--yellow` / `--red` / `--teal` | טוקנים | בינונית |
| **7** | `--font-display` (Anton + Rubik) בכותרות | הדרגתי | בינונית |
| **8** | קו רכס מעל הפוטר + קשתות שמש בסקשנים נבחרים | הדרגתי | בינונית |
| **9** | צללית מנוף בנקודה אחת | נקודתי | נמוכה-אך-נחמדה |

---

*הצעה בלבד. שום קובץ באתר לא שונה.*
