# Design QA — קבוצה 2: about · contact · faq · merch · plaza

בדיקת מעצב (audit בלבד — לא בוצעו שינויים בקבצים). נבדק בשלושה רוחבים: דסקטופ ~1440px, טאבלט ~768px, מובייל ~390px, מול השרת המקומי `http://localhost:8080`.

**הערה מתודולוגית חשובה:** Chrome headless במצב הישן כופה רוחב מינימלי של ~500px, כך שצילומי "390px" רגילים מציגים פריסה של 500px חתוכה לתוך קנבס 390 — מה שנראה כאילו יש גלישה אופקית וקטיעה בצד ימין. אימתתי מול מדידת `innerWidth`/`scrollWidth` אמיתית ומול device‑emulation: **אין גלישה אופקית אמיתית באף אחד מחמשת העמודים** (`scrollWidth` < `innerWidth` בכל הרוחבים). כל "קטיעה" שנראית בצילומי 390/768 הגולמיים היא ארטיפקט צילום, לא באג.

---

## ממצאים חוצי-עמודים (Nav / Footer / פלטת צבעים — משפיעים על כל החמישה)

### Major
- **ניגודיות טקסט `--muted` (#8A7256) נכשלת ב-WCAG AA.** חושב מהצבעים בפועל:
  - על `--bg` (#FAF4E9): **4.15:1** (נדרש 4.5 לטקסט רגיל)
  - על `--bg2` / גרדיאנט ה-hero הבהיר: **3.77:1**
  - על `--card-bg` (#FFFDF8): **4.47:1** (נכשל בגבול)
  זהו הצבע של: תת-כותרות ה-hero (`.page-hero-sub` / `.hero-sub`), פסקאות גוף, טקסט עזר בטופס, תיאורי מוצר (`.vinyl-banner-desc`, `.shop-note`), תשובות ה-FAQ, וטקסט הפוטר. כיוון שרובו ב-16px (לא "large text"), הסף הוא 4.5 — ולכן נכשל. זהו הממצא הנגישותי המשמעותי ביותר בקבוצה, והוא מערכתי (פלטה משותפת). המלצה: להכהות את `--muted` ל-~#6E5A44 כדי לעבור 4.5 על שני הרקעים.

### Minor
- **קישורי teal (#0A8F8E) בתשובות ה-FAQ**: 3.6:1 על bg / 3.87:1 על card — נכשל 4.5 לקישור בגודל רגיל.
- **כתום כטקסט קטן**: `--orange` על bg = 3.48:1, על card = 3.75:1. מחירים (1.2rem+ bold) עוברים כ-"large text" (3:1) — אבל קישורי "לרכישה ←" (0.85rem) ותווית ה-ticker (0.7rem, 3.85:1) הם טקסט רגיל ונכשלים.
- **טקסט לבן על כפתורי CTA כתומים**: לבן על העצירה הבהירה של הגרדיאנט (#E0560B) = **3.75:1**; על העצירה הכהה (#c43d0e) = 5.14:1. מאחר שהכפתורים הם גרדיאנט, החלק הבהיר של הכפתור מציג טקסט מתחת ל-4.5. משפיע על "כרטיסים", "שליחת הודעה", "צור קשר" וכפתורי הקנייה. הכהיית נקודת ההתחלה של הגרדיאנט תפתור.
- **מטרת מגע (touch target) של ההמבורגר**: `.hamburger` הוא ~24×16px, הרבה מתחת ל-44px המומלץ. משותף ל-nav בכל העמודים.

### Polish
- **ניגודיות placeholder** (rgba(138,114,86,.6)) = 2.16:1 — נמוך מאוד. לא חוסם כי לכל שדה יש `<label>` אמיתי, אבל כדאי להכהות מעט.
- **קוהרנטיות בין ה-hero-ים**: contact + about משתמשים ב-hero **כהה** (#1a1410, כותרת לבנה); faq + merch + plaza משתמשים ב-hero **בהיר** (קרם, כותרת כהה עם accent בגרדיאנט). המעבר בין העמודים מרגיש מעט לא-אחיד. אין חלוקה לוגית ברורה (contact "שירותי" כמו faq אך מקבל טיפול כהה). כדאי להחליט על שפה אחת, או להצדיק את ההבחנה במכוון.

---

## contact.html

עמוד חזק ומלוטש. הטופס מיושם ברמה נגישותית גבוהה.

### תקין / חיובי
- כל שדה עם `<label for>` תואם ל-`id` ✓, `aria-required="true"` על שדות חובה ✓, סימון חובה ויזואלי (`*` + `.req`) ✓.
- הודעות שגיאה לכל שדה (`.field-error`) + ניקוי שגיאה תוך כדי הקלדה + `role="status"` `aria-live="polite"` להודעת ההצלחה ✓.
- מצבי focus גלובליים (`:focus-visible`) פעילים ✓. שדות קלט בגובה ~55px, כפתור שליחה גדול — מטרות מגע טובות ✓.
- מובייל וטאבלט: הטופס קורס לעמודה אחת נקי, הפוטר נערם יפה. אין גלישה.

### Minor
- **סדר כותרות לא לינארי**: ב-DOM ה-`<aside>` מכיל `<h3>` ("אנחנו כאן בשבילכם") *לפני* ה-`<section>` עם `<h2>` ("נשמח לשמוע מכם") — כלומר h1 → h3 → h2. שני אלה הם כותרות ברמת-סקשן; כדאי שה-aside יהיה `<h2>` (או ליישר את ההיררכיה) כדי למנוע דילוג רמה.
- **תיבת הסימון (checkbox)** 24×24px — קטנה מ-44px כמטרת מגע, אך ה-`<label>` כולו לחיץ ומרכך זאת.
- ניגודיות muted/placeholder (משותף).

**מסקנה:** העמוד הכי בשל בקבוצה מבחינת נגישות טופס. רק תיקון ניגודיות + סדר כותרות.

---

## about.html

עמוד נקי ומלוטש בכל שלושת הרוחבים. hero כהה, סקשני סיפור, כרטיס מנהלת אמנותית, וגלריית תמונות.

### תקין / חיובי
- היררכיית כותרות תקינה: h1 → h2 ×3, ללא דילוגים ✓.
- כל תמונות הגלריה עם `alt` תיאורי ומשמעותי ✓.
- הגלריה (grid 3-עמודות בדסקטופ → 2 במובייל, `grid-auto-rows`) נראית מסודרת, ללא מעיכה/חיתוך.

### Minor
- **lightbox עם `alt=""` ריק**: `<img class="lb-img" id="lbImg" alt="">` — ה-src נקבע דינמית בפתיחה, אבל ה-alt נשאר ריק. כדאי לעדכן את ה-`alt` (או `aria-label` על ה-dialog) לטקסט ה-caption בעת הפתיחה.
- ניגודיות טקסט גוף `--muted` (משותף).

**מסקנה:** במצב טוב. רק ה-alt של ה-lightbox וניגודיות ה-muted.

---

## faq.html

עמוד מלוטש עם נגישות אקורדיון מצוינת.

### תקין / חיובי
- **אקורדיון נגיש כראוי**: כל כפתור `.faq-q` הוא `<button>` עם `aria-expanded` (מתחלף אמיתית ב-JS, שורה ~854) ו-`aria-controls` המצביע על `id` תואם בפאנל התשובה — כל 12 הזוגות מותאמים ✓.
- היררכיית כותרות: h1 → h2 לכל קטגוריה ✓. מצבי focus גלובליים על הכפתורים ✓.
- אייקון ה-"+" הוא CSS דקורטיבי (לא נכנס ל-accessible name) ✓.
- מובייל/טאבלט: פריסה נקייה, בלוק ה-CTA התחתון מסודר.

### Minor
- **קישורי teal בתשובות** = 3.6:1 — נכשל 4.5 (ראה משותף).
- ניגודיות טקסט תשובות `--muted` (3.77–4.15) — נכשל (משותף). זה בולט כאן במיוחד כי גוף התוכן הוא כולו muted.

**מסקנה:** מבנה ונגישות מצוינים; רק ניגודיות צבעים.

---

## merch.html

רשת מוצרים יפה ותואמת לשפת דף-הבית. יש באג overlap אמיתי בטאבלט.

### Major
- **תמונת ה-vinyl ב-hero גולשת מעל הכותרת ותת-הכותרת (~520–980px, כולל 768).** אומת בצילום window-size=768 אמיתי (לא ארטיפקט): תמונת התקליט מכסה חלקית את "חנות הפסטיבל" וכמעט לגמרי את "קולקציית המרצ׳נדייז...".
  - **גורם:** `.vinyl-banner-media img` הוא `position:absolute; bottom:32px; width:88%; height:auto`. גובה התמונה (יחס גובה-רוחב) גדול מ-`min-height:260px` של `.vinyl-banner-media`, ולכן היא גולשת כלפי מעלה. במעבר ל-stacked (`order:-1`, מדיה למעלה) הגלישה מתנגשת ב-`.page-hero` שמעליה.
  - **סלקטורים לבדיקה:** `.vinyl-banner-media`, `.vinyl-banner-media img`, וה-media query `@media (max-width: 980px)`. פתרון אפשרי: לתת ל-`.vinyl-banner-media` `min-height` דינמי / `aspect-ratio` / להפוך את התמונה ל-`position:static` בפריסה הנערמת, או להוסיף `padding-top` ל-hero.

### Minor
- **אין כותרות סמנטיות למוצרים**: שמות המוצרים הם `<div class="shop-card-name">`, ובכל העמוד יש רק ה-h1 — אין אף h2/h3. לצורכי a11y/SEO כדאי ששמות המוצרים (או כותרת הסקשן "חנות הפסטיבל") יהיו כותרות.
- **קישורי "לרכישה ←"** (0.85rem, כתום) = 3.75:1 — נכשל כטקסט רגיל (מחירים ב-1.2rem bold עוברים כ-large text). ראה משותף.

### תקין / חיובי
- כל תמונות המוצר עם `alt` תיאורי ומשמעותי ✓.
- דסקטופ (3 עמודות) ומובייל (עמודה אחת) — נקי, הכרטיסים אחידים, יחסי תמונה 1:1 עקביים.

**מסקנה:** במצב טוב פרט ל-overlap של ה-hero בטאבלט — זה הפריט היחיד שדורש תיקון ממשי בקבוצה.

---

## plaza.html

עמוד נקי ומלוטש. פריסת image/text מתחלפת עם 6 אזורים.

### תקין / חיובי
- היררכיית כותרות: h1 → h2 ×6 (אחד לכל אזור) ✓.
- כל 6 תמונות האזורים עם `alt` תיאורי ומשמעותי ✓.
- דסקטופ / טאבלט / מובייל — כולם נקיים; הפריסה המתחלפת נערמת יפה (תמונה מעל טקסט) במובייל, ללא חיתוך/גלישה.

### Minor
- ניגודיות טקסט גוף `--muted` (משותף).

**מסקנה:** בפורמה טובה. אין באג ספציפי לעמוד — רק הניגודיות המערכתית.

---

## פסק דין כולל לקבוצה

הקבוצה במצב **טוב עד טוב-מאוד** ומרגישה מקצועית ואחידה בשפת המותג (קרם חם, כתום/אדום, כרטיסים מעוגלים, אותו nav/ticker/footer). היררכיית מסכים, יישור RTL, ומצבי focus תקינים. **אין גלישה אופקית אמיתית** באף עמוד.

שני נושאים מרכזיים ורוחביים:
1. **ניגודיות (Major, מערכתי):** `--muted` נכשל AA (4.15/3.77/4.47), וכן teal ה-FAQ, כתום קטן וטקסט לבן על העצירה הבהירה של גרדיאנט הכפתורים. תיקון פלטה נקודתי (הכהיית `--muted` ו-נקודת התחלת גרדיאנט הכתום) מתקן את רוב הממצאים בכל חמשת העמודים בבת אחת.
2. **overlap ב-merch (Major, נקודתי):** תמונת ה-vinyl מתנגשת בכותרת ה-hero ב-~768. הבאג הממשי היחיד.

מעבר לכך: התאמות Minor — סדר כותרות ב-contact, `alt` ל-lightbox ב-about, כותרות סמנטיות למוצרים ב-merch, מטרת-מגע של ההמבורגר, והחלטה על שפת hero אחידה (כהה מול בהיר). אין ממצאים קריטיים.
